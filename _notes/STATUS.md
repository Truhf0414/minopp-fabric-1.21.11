# STATUS — minopp port

- **當前 phase**: **Phase 3 收尾＝port 主體完工（2026-06-25）**——runtime 完整驗證五子批（S0 build sanity / S1 玩法 / S2 Bot＋config / S3 NBT 存讀 / S4 視覺 / S5 多人冒煙）全綠，**全程 0 行 code change**（純驗證；逮到的洞全是 dev server 環境設定與 vanilla 機制、非 minopp）。`ActionMessage` JSON-in-NBT 實測通過、B TODO 牌堆不閃定案保留、忠實 port 邊界（A/B/ActionMessage/nobewlr）全守住。全 log **0 ERROR**。**Phase 0/1/2/3 全收尾、port 主體完成。**
- **最後 build/run 綠的 checkpoint**:
  - 步驟5a runClient 空殼進主畫面（2026-06-24，bare officialMojangMappings + 單 source set + `--refresh-dependencies`）。
  - 步驟5b build **預期 FAILED**：加 YACL + 修 fabric-api 7 個後 = **38 個真 1.21.11 MC API error**（render/HUD/SpecialModelRenderer/Screen/Entity/Player；是 Phase 1/2 待辦、非壞）。完整 log: `build-5b-fabricapi.log`。
  - **phase-1 收尾（2026-06-24）**：compileJava **35 errors**（server 3 個精確消失、無新增 server diag、剩餘逐條全 client/render；非綠是預期、client 屬 Phase 2）。log: `compile-phase1-after.log`。
  - **phase-2 收尾（2026-06-24）**：compileJava **35→0** + 完整 `build` **SUCCESSFUL**（A 手牌7〔35→28〕/ B render-state9〔28→19〕/ C HUD+Screen19〔19→0〕，逐子批 grep 精確消、無新增、各檔歸零）。log: `compile-phase2-A/B/C-*.log`、`build-phase2-final.log`。
- **當前對齊的 upstream commit**: `a0dc41e`（2026-06-01）；攤平切片 (fabric, >=1.21.2, <26.1)；base=**B**（26.1.2-fabric 新碼）唯讀複製。
- **環境真相（步驟5a 釘死，勿再翻）**:
  - **mapping = bare `loom.officialMojangMappings()`**（前 session 拍板正確，runClient 實證）。skill 的 layered 錯方向（實測也炸）。
  - **source set = 單一（src/main）**〔2026-06-24 拍板〕，不採官方模板 split。upstream 本就單 source set + @Environment/entrypoint 分流。
  - **⚠️ intermediary cache**: runClient crash 於 `class_XXXX not found`（一片 Fabric API entrypoint 掛）= cache 缺 intermediary，`gradlew <task> --refresh-dependencies` 補上即解（與配置無關，別改 bare）。本機已補。
  - env：loader 0.19.3 / loom 1.17-SNAPSHOT(→1.17.12) / fabric-api 0.141.4+1.21.11 / **YACL 3.8.1+1.21.11-fabric**（isxander maven；3.8.2 未上 maven 勿用）。

- **phase-0 產出總結（步驟5b/6 完成）**:
  - 60 class 唯讀複製進 src/main（母數核對通過：60、neoforge 0、15 package 分佈全對）。
  - 資源母數全對。**pack.mcmeta 純 Fabric 不需要**（非漏搬；官方 fabric-example-mod 1.21.11 模板實證不帶；conventions 母數已修正）。
  - **build error 178 →（加 YACL）45 →（修 fabric-api 7 個命名回退）38**。38 全是真 MC API。
  - base=B 經 5b 編譯器證實**站得住**（SpecialModelRenderer/CameraRenderState 等存在、僅簽名/package 微調；證偽閘未觸發）。
  - fabric-api 7 個已修（RegistriesWrapperImpl/ClientPlatform/CompatPacketRegistry 回退 `<26.1` 舊命名 itemgroup/keybinding/playC2S，jar+javap 三方驗）。

- **38 error 全貌分類（→ Phase 1/2 切分，詳見 api-changes.md「步驟5b 後續」段）**:
  - **Phase 1（server，3）**: EntityAutoPlayer 2（:215 override + :238 interact 簽名）、PlayerShim 1（sendSystemMessage(Component)）。
  - **Phase 2（client/render，35）**: render-state（BlockEntityMinoTableRenderer 7 + EntityAutoPlayerRenderer 2）、HUD/GuiGraphics（GuiShim 7 + GameOverlayLayer 6）、SpecialModelRenderer.Unbaked（HandCardsSpecialRenderer 6 + MinoFabricClient 1）、Screen override（SeatControlScreen 3 + WildSelectionScreen 3）。

- **下一步：port 主體已完工、無 phase 待辦**——剩兩件皆 out-of-scope：① **發布**（conventions 要 push；mod-fork-publish 流程，使用者決定何時推）；② **長期維護**（upstream 出新版 diff-backport，另一條維護線、非本 port phase）。S5 完整多人並發壓測（多真人 race／網路延遲）亦 out-of-scope（冒煙已過）。2 個 upstream 作者 TODO（BlockEntityMinoTableRenderer:256 / ItemHandCards:101）＋ ActionMessage JSON-in-NBT ＋ nobewlr lang：Phase 3 查證後**全保留不改**（玩家無感、忠實 port 邊界）。
- **✅ mixin crash 已解（phase-2b 2026-06-25）**: MouseHandlerMixin `@Local(name="wheel")` → `@Local(index=15)`。**root cause 修正**（推翻舊說法）：**非「mojmap 剝離 local 名」**——jar 其實有 LVT（javap -l 證 35 method 帶）；真因是官方 mojmap 該 local 名為 `k`、upstream by-name "wheel" 來自 Parchment（NeoForge 環境補 local 名），bare officialMojangMappings 無 Parchment 故對不上。javap -l 證 `onScroll` slot 15:`I`(唯一 int)、slot 7:`Z`(boolean、不算 int) → 用 `index` 直鎖 slot 比 `ordinal` 穩（ordinal 受 runtime 有無 LVT 影響）。其餘 3 mixin（InventoryMixin 攤平後死碼 / KeyMappingAccessor / AbstractClientPlayerMixin）runClient required=1 蓋章。詳 api-changes/learnings phase-2b。
- **Phase 2 範式發現（2026-06-24，⚠️ v3 javap mojmap 大修正）**: v2 eng-review「範式①（render 類 SubmitNodeCollector→OrderedRenderCommandQueue、走 queue.submitXXX）」+「render-state method updateRenderState」**已被 v3 javap 推翻**——那些是 **yarn 名**，前 session spike 拉 MCP yarn 源沒換 mojmap。mojmap 真相：A/B 的 `submit`/`SubmitNodeCollector`/`extractRenderState` **26.1↔1.21.11 沒改名**；手牌真實差異 = submit 補 ItemDisplayContext 參數 + Unbaked 去泛型，方塊桌 BlockEntityRenderer 介面**與現狀全等**、error 在周邊 class mojmap 命名。v2 範式②（GUI GuiGraphicsExtractor→GuiGraphics 退回）**已 C1 javap 重驗完成**：型別 GuiGraphics 對，但 method 名有**二階陷阱**（text→drawString / centeredText→drawCenteredString / Screen extractBackground→renderBackground，26.1 新名 vs 1.21.11 舊名；詳見 api-changes/changelog phase-2 段）。手牌主路徑 SpecialModelRenderer 走到底**不退 BEWLR**（成立）。詳見 plans/phase-2.md「範式修正」+ learnings「yarn 結構≠mojmap code」「二階命名陷阱」。
- **🔖 新 session 續接提醒**: 在 `H:\MC_Mods_Port\Minopp_Port\Minopp` 開 claude（CLAUDE.md 自動路由 workflow）；按序讀 STATUS → api-changes → conventions → learnings → changelog → plans/。**Phase 0/1/2/3 全收尾＝port 主體完成**；新對話 = **執行 public 前 code review（spec 定稿 `plans/review.md`），下一步 Phase 0 git baseline**（丙方案、見下方 2026-06-26 區塊）；發布/維護待 review 收完。mojmap 查證法（已驗）：javap loom cache mojmap jar `...layered+hash.2198-v2`（`-l` 看 LVT 定 @Local slot/型別；javap=`C:\Program Files\Java\jdk-17\bin\javap.exe`）；MCP mojmap decompile 不可用（pitfall-log 模式 B），yarn 只看結構不當 code 名。⚠️ **多人 runtime 測試別用 agent background server**（搆不到 console、改設定狂重啟；該使用者本機自起，learnings phase-3）。upstream 唯讀參考在隔壁 `Minopp-upstream`。
- **🆕 2026-06-26 RTK 環境坑（git 不可信，重啟前必讀）**: RTK（全域 Bash hook）對 **git** 命令造假——`git add` 等 mutating **不反映到真 .git**、`git diff/status` 回假數據（實測謊報 110 staged、真 git 看 0）；對 gradlew/一般命令正常。已從 `~/.claude/settings.json` 移除 `rtk hook claude`（備份 `settings.json.bak`），**但需重啟 Claude Code 才生效**；重啟前 **git 一律走 PowerShell tool**（真 `git.exe`，RTK 只攔 Bash）。詳見 memory `rtk-fakes-git`。**Phase 0 git baseline 尚未真正落地**（先前 Bash 的 git init/add 是 RTK 假數據；真 git 現況：repo 已 init、staged=0、0 commit、全 untracked）→ 重啟後用真 git 重做 Phase 0 staged gate + baseline commit（**不掛 Co-Authored-By**，memory `commit-no-coauthor`）。
- **本 phase retro（2026-06-24 步驟5b/6）**: 2 條新學習已記 learnings — ① **base=B 是「混合基準」**（MC API 用 B、fabric-api 子集回退 `<26.1` 舊命名）；② **pack.mcmeta 純 Fabric 不需要**（母數別把 modstitch 多平台 template 當純 Fabric 必需）。另：upstream 原碼有 2 個作者 TODO（ItemHandCards:101 / BlockEntityMinoTableRenderer:256 "Do the extraction"），屬 Phase 1/2 對齊時碰、非 phase-0 引入。
- **本 phase retro（2026-06-24 phase-1）**: 1 條新學習已記 learnings — **查證工具鏈 fallback：編譯器是最硬且永遠在的 ground truth**（decompile MCP 壞 + javap 撈不到 mojmap jar，改「直接改→compileJava」查死 displayClientMessage）。救援時點：structured-plan-review codex+self 一致抓到 EntityAutoPlayer NBT 缺 @Override 的 silent-failure 風險 → 補回 → 編譯器確認簽名真匹配。
- **本 phase retro（2026-06-24 phase-2）**: 1 條新學習已記 learnings — **範式②「換型別後才暴露的二階命名陷阱」**（型別存在性=一階查證、換型別後 method 簽名=二階查證，javap 兩階一起釘；救援時點：plan C1 強制 javap 重驗擋下「只換型別→method 名整批爆」）。零誤傷、三子批逐檔精確消，純機械替換為主、無 runtime 意外。
- **本 phase retro（2026-06-25 phase-2b，有意外、非機械 phase）**: 4 條新學習已記 learnings — ① **@Local ordinal/index 看 LVT 型別**（javap -l；slot 7 boolean 非 int、index 比 ordinal 穩）；② **mixin「真生效」驗證需完整遊戲 setup**（非啟動不 crash 就完，滾輪翻牌要綁定牌局手牌）；③ **runtime 行為+視覺車道逮 2 個 upstream 原生完整性洞**（coupon item definition + recipe ingredient，green build 全綠抓不到）；④ 誠實：**Glob No files found 路徑假象**、自我推翻 datagen 誤推。pitfall-log 新增**模式 F**（忠實 port 連 upstream 原生洞搬入）+ phase-2b 坑帳 + 後設問題 5（忠實 port 邊界）。救援時點：javap -l 推翻中途 ordinal=1、structured-plan-review codex 抓 S3「mixin apply log 期待」、runtime 車道逮 coupon+recipe。
- **本 phase retro（2026-06-25 phase-3，verification-heavy／0 code change）**: 2 條新學習已記 learnings — ① **多人 runtime 測試別用 agent background server**（搆不到 console 不能 op、改設定狂重啟、使用者反覆重連 5+ 次 → 學長喊停自己起；該預鋪 server 設定＋使用者本機起）；② **多人「要 OP」先查 mod code 別假設 mod 設計**（入座 code 零權限、真因 vanilla spawn-protection）。0 code change：逮到的洞全是 dev server 環境串（eula/online-mode/enforce-secure-profile/spawn-protection）＋ vanilla 機制，minopp 本體零問題。撈到 plan 漏的玩法（award/demo/shout/cut）已驗納記錄；忠實 port 邊界（A/B/ActionMessage/nobewlr）查證後全保留。pitfall-log 新增 phase-3 坑帳（環境/協作模式坑）。
- **🆕 2026-06-26 public 前 code review 啟動（spec 定稿、未動工）**: port 主體完工後、public 前的品質關卡（**非 port phase**）。
  - **定調 = 丙方案**（放棄嚴格忠實 port、趁機清乾淨成好維護的 fork）；忠實 port 邊界約束**解除**（A/B/ActionMessage/nobewlr 等現「可動」，但「改遊戲行為」走 Phase D gate 學長拍）。
  - **spec 真相源 = `plans/review.md`**（git baseline Phase 0 / 60 class 切 6 批 / 三檔 P0-P2 / stop line「不為改而改」/ 修改分類→重驗強度 / finding schema / DoD / Phase 0→F 流程）。
  - **動工前 gate 已過**：我自檢 + codex outside voice（`invoking-codex-cli`，xhigh）；codex 採納 6 項已補 spec、駁回 1（逐批 pipeline，理由見 spec）、提前 1（**LICENSE→P0**）；證據 `plans/review-codex-outside-voice.md`。
  - **license 查證**: upstream MIT（Copyright 2024 Zbx1425）〔查過 LICENSE.txt〕；散布合法、唯一義務=保留原 MIT+copyright。⚠️ **工作目錄目前無 LICENSE.txt（攤平漏帶），P0 必補**。發 PR upstream 不可行（攤平單版 vs upstream stonecutter 多版多 loader、形態不相容）→ 走 public fork。
  - **下一步 = Phase 0 git baseline**（此目錄**非 git repo**）：git init + 寫 `.gitignore`（⚠️ `gradle-wrapper.jar` 要 commit）+ 🛑 staged 清單審核 gate（commit 前給學長看 `git diff --cached`）+ baseline commit「忠實 port 快照」（**排除** review.md/notes/LICENSE）。
- **最後更新**: 2026-06-26（**public 前 code review spec 定稿、未動工**：丙方案、spec=`plans/review.md`、codex outside voice 過、LICENSE 進 P0、下一步 Phase 0 git baseline）。前次 2026-06-25（**Phase 3 收尾＝port 主體完工**：runtime 完整驗證五子批 S0-S5 全綠、0 code change、全 log 0 ERROR；ActionMessage JSON-in-NBT 實測過、B TODO 定案保留、忠實 port 邊界全守住；逮到的洞全非 minopp（dev server 環境串＋vanilla spawn-protection）。plan: plans/phase-3.md；changelog/learnings/pitfall-log/STATUS 全已記。**Phase 0/1/2/3 全收尾、port 主體完成、無 phase 待辦**）。前次：phase-2b＝Phase 2 真收尾（mixin runtime + E3 視覺 + 2 upstream 洞）。
